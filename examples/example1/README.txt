
This example read the specified source code and create a
map file with information of used namespaces and defined
functions. Also, create a "dof" graph format file that
can be ploted.


To compile use,

$ bash compile.sh

To execute,

./example1 toMap/code1.c
